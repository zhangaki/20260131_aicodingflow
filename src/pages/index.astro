---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import BackgroundEffects from '../components/BackgroundEffects.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog')).sort(
	(a, b) => {
		const dateA = a.data.pubDate instanceof Date ? a.data.pubDate : new Date(a.data.pubDate);
		const dateB = b.data.pubDate instanceof Date ? b.data.pubDate : new Date(b.data.pubDate);
		return dateB.valueOf() - dateA.valueOf();
	}
);
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>

			main { width: 100%; max-width: 1200px; margin: 0 auto; padding: 2rem 1rem; position: relative; }
			
            /* Hero Section */
            .hero {
                padding: 4rem 0;
                text-align: center;
                max-width: 1000px;
                margin: 0 auto;
                position: relative;
            }
            .hero-badge {
                display: inline-block;
                padding: 0.4rem 1rem;
                background: rgba(255, 255, 255, 0.05);
                border: 1px solid rgba(255, 255, 255, 0.1);
                border-radius: 100px;
                color: #d4d4d8;
                font-size: 0.75rem;
                font-weight: 700;
                text-transform: uppercase;
                letter-spacing: 0.05em;
                margin-bottom: 2rem;
                margin-top: 1.5rem;
            }
            .hero h1 {
                font-size: 4.5rem;
                line-height: 1;
                font-weight: 800;
                letter-spacing: -0.04em;
                margin-bottom: 1.5rem;
                background: linear-gradient(to bottom, #fff 60%, rgba(255,255,255,0.4));
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
            }
            .hero-subtitle {
                font-size: 1.25rem;
                color: #a3a3a3;
                line-height: 1.6;
                margin-bottom: 3rem;
                max-width: 700px;
                margin-left: auto;
                margin-right: auto;
            }

            /* Bento Grid */
            .featured-sections { margin: 6rem 0; }
            .featured-sections h2 { font-size: 2.5rem; margin-bottom: 3rem; text-align: center; }
            .bento-grid {
                display: grid;
                grid-template-columns: repeat(4, 1fr);
                grid-template-rows: repeat(2, minmax(260px, auto));
                gap: 1.5rem;
            }
            .bento-item {
                position: relative;
                padding: 2rem;
                border-radius: 40px;
                border: 1px solid rgba(255,255,255,0.05);
                background: linear-gradient(135deg, rgba(23, 23, 23, 0.4) 0%, rgba(10, 10, 10, 0.6) 100%);
                backdrop-filter: blur(30px) saturate(180%);
                transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
                display: flex;
                flex-direction: column;
                justify-content: flex-start;
                text-decoration: none;
                color: inherit;
                overflow: hidden;
            }
            .bento-item::after {
                content: "";
                position: absolute;
                inset: 0;
                background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
                opacity: 0.03;
                pointer-events: none;
            }
            
            .bento-item:nth-child(1) { 
                grid-column: span 2; 
                grid-row: span 2; 
                justify-content: space-between; 
                padding: 2.5rem;
            }
            .bento-item:nth-child(2) { 
                grid-column: span 2; 
                grid-row: span 1; 
                flex-direction: row; 
                align-items: stretch; /* Fill height */
                padding: 0; /* Remove default padding for internal split */
            }
            .bento-item:nth-child(3) { 
                grid-column: span 2; 
                grid-row: span 1; 
                flex-direction: row; 
                align-items: stretch; /* Fill height */
                padding: 0;
            }
            
            /* Internal content for small cards */
            .small-card-content {
                flex: 1.2;
                padding: 2rem;
                display: flex;
                flex-direction: column;
                justify-content: center;
                border-right: 1px solid rgba(255,255,255,0.05);
            }
            .small-card-visual {
                flex: 1;
                position: relative;
                overflow: hidden;
                display: flex;
                align-items: center;
                justify-content: center;
                background: rgba(0,0,0,0.2);
            }
            .small-card-visual img {
                width: 100%;
                height: 100%;
                object-fit: cover;
                opacity: 0.8;
                transition: transform 0.6s cubic-bezier(0.16, 1, 0.3, 1);
            }
            .bento-item:hover .small-card-visual img {
                transform: scale(1.1) rotate(2deg);
                opacity: 1;
            }

            /* Technical Metadata Decoration */
            .bento-meta {
                position: absolute;
                bottom: 2.5rem;
                right: 2.5rem;
                font-family: monospace;
                font-size: 0.7rem;
                color: rgba(163, 163, 163, 0.4);
                text-transform: uppercase;
                letter-spacing: 0.1em;
                display: flex;
                flex-direction: column;
                align-items: flex-end;
                pointer-events: none;
            }
            .bento-meta span { margin-top: 0.3rem; }
            .bento-badge {
                position: absolute;
                top: 2.5rem;
                right: 2.5rem;
                font-size: 0.65rem;
                background: rgba(255,255,255,0.05);
                border: 1px solid rgba(255,255,255,0.1);
                color: #a3a3a3;
                padding: 0.2rem 0.6rem;
                border-radius: 6px;
                font-weight: 600;
            }

            .bento-item h3 { font-size: 1.8rem; color: #fff; margin: 0 0 1rem 0; font-weight: 800; letter-spacing: -0.04em; }
            .bento-item p { color: #a3a3a3; margin: 0; line-height: 1.6; font-size: 1.05rem; }

            /* Large Card Image Adjustment */
            .bento-item:nth-child(1) .scan-container {
                margin-top: 3rem;
                width: calc(100% + 1rem);
                margin-left: -0.5rem;
                aspect-ratio: 16/9;
            }

            /* Image Scan Effect */
            .scan-container {
                position: relative;
                overflow: hidden;
                border-radius: 24px;
                margin-top: 2rem;
                width: 100%;
                aspect-ratio: 16/10;
                background: rgba(0,0,0,0.5);
                border: 1px solid rgba(255,255,255,0.1);
                box-shadow: 0 20px 40px rgba(0,0,0,0.3);
            }
            .scan-container img {
                width: 100%;
                height: 100%;
                object-fit: cover;
                display: block;
            }
            .scan-container::after {
                content: '';
                position: absolute;
                top: 0;
                left: -100%;
                width: 50%;
                height: 100%;
                background: linear-gradient(
                    90deg,
                    transparent,
                    rgba(255, 255, 255, 0.2),
                    transparent
                );
                transform: skewX(-20deg);
                animation: scan-highlight 4s infinite cubic-bezier(0.4, 0, 0.2, 1);
            }
            @keyframes scan-highlight {
                0% { left: -100%; }
                40% { left: 200%; }
                100% { left: 200%; }
            }

            /* Testimonials Marquee */
            @keyframes marquee-scroll {
                0% { transform: translateX(0); }
                100% { transform: translateX(-50%); }
            }

            .marquee-wrapper {
                margin: 4rem 0;
                overflow: hidden;
                position: relative;
                width: 100%;
                -webkit-mask-image: linear-gradient(to right, transparent, black 10%, black 90%, transparent);
                mask-image: linear-gradient(to right, transparent, black 10%, black 90%, transparent);
            }
            .marquee-track {
                display: flex !important;
                flex-direction: row !important;
                gap: 1.5rem;
                width: max-content; 
                animation: marquee-scroll 40s linear infinite;
                padding: 1rem 0;
            }
            .marquee-track:hover {
                animation-play-state: paused;
            }
            .review-card {
                background: rgba(23, 23, 23, 0.4);
                border: 1px solid rgba(255, 255, 255, 0.1);
                backdrop-filter: blur(10px);
                border-radius: 20px;
                padding: 1.5rem;
                width: 320px !important;
                min-width: 320px;
                flex-shrink: 0;
                display: flex;
                flex-direction: column;
                gap: 1rem;
            }
            .reviewer-header {
                display: flex;
                align-items: center;
                gap: 0.8rem;
                width: 100%;
            }
            .avatar {
                width: 44px;
                height: 44px;
                min-width: 44px;
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
                font-weight: 800;
                color: rgba(255,255,255,0.9);
                flex-shrink: 0;
            }
            .reviewer-info {
                display: flex;
                flex-direction: column;
            }
            .reviewer-name { font-size: 1rem; font-weight: 700; color: #fff; }
            .reviewer-role { font-size: 0.75rem; color: #a3a3a3; text-transform: uppercase; letter-spacing: 0.05em; }
            .review-text { margin: 0; color: #d4d4d8; font-size: 0.95rem; line-height: 1.6; font-style: italic; }

            /* Latest Insights (Post Grid) */
			.latest-insights { margin-top: 6rem; border-top: 1px solid rgba(255,255,255,0.1); padding-top: 4rem; }
            .latest-insights h2 { font-size: 2.5rem; margin-bottom: 2rem; text-align: center; }
			.posts-grid { 
                display: grid; 
                grid-template-columns: repeat(auto-fill, minmax(350px, 1fr)); 
                gap: 2rem; 
                margin-top: 2rem;
                /* Optimize rendering for many items */
                content-visibility: auto;
                contain-intrinsic-size: 1000px;
            }
			.post-card { 
                position: relative;
                display: flex; 
                flex-direction: column; 
                border-radius: 24px; 
                overflow: hidden; 
                text-decoration: none; 
                background: linear-gradient(135deg, rgba(23, 23, 23, 0.3) 0%, rgba(10, 10, 10, 0.5) 100%);
                border: 1px solid rgba(255, 255, 255, 0.05); 
                backdrop-filter: blur(20px);
                transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1); 
                height: 100%; 
            }
            .post-card::after {
                content: "";
                position: absolute;
                inset: 0;
                background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
                opacity: 0.02;
                pointer-events: none;
            }
			.post-card:hover { 
                transform: translateY(-10px) scale(1.02); 
                border-color: rgba(255, 255, 255, 0.2);
                box-shadow: 0 30px 60px -12px rgba(0, 0, 0, 0.6);
                background: rgba(23, 23, 23, 0.5);
            }
			.post-image { height: 220px; background: rgba(0, 0, 0, 0.4); overflow: hidden; position: relative; }
            .post-image::after {
                content: '';
                position: absolute;
                inset: 0;
                background: linear-gradient(to bottom, transparent 60%, rgba(10, 10, 10, 0.8));
            }
			.post-image img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.5s ease; }
            .post-card:hover .post-image img { transform: scale(1.1); }
			.post-content { padding: 1.8rem; flex-grow: 1; display: flex; flex-direction: column; justify-content: space-between; position: relative; z-index: 1; }
			.post-content h3 { margin: 0 0 0.8rem 0; color: #fff; font-size: 1.4rem; line-height: 1.4; font-weight: 700; letter-spacing: -0.02em; }
			.post-content p { color: #a3a3a3; font-size: 1rem; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; line-height: 1.6; margin: 0; }

            @media (max-width: 720px) {
                main { padding: 1.5rem 0.5rem; }
                .hero { padding: 3rem 0 1rem 0; }
                .hero h1 { 
                    font-size: 2.4rem; 
                    line-height: 1.1;
                    letter-spacing: -0.02em;
                    padding: 0 0.5rem;
                }
                .hero-subtitle {
                    font-size: 1.1rem;
                    padding: 0 1rem;
                    margin-bottom: 2rem;
                }
                .featured-sections { margin: 3rem 0; }
                .featured-sections h2 { font-size: 1.8rem; margin-bottom: 2rem; }
                .bento-grid { 
                    grid-template-columns: 1fr; 
                    gap: 1.5rem; 
                }
                .bento-item { 
                    flex-direction: column !important; 
                    padding: 1.5rem; 
                    border-radius: 32px; 
                }
                .small-card-content {
                    padding: 0.5rem 1.5rem;
                    flex: none;
                }
                .bento-item:nth-child(1), .bento-item:nth-child(2), .bento-item:nth-child(3) { grid-area: auto; grid-column: span 1; grid-row: span 1; }
                .bento-item:not(:nth-child(1)) .scan-container { width: 100%; margin-top: 1.5rem; }
                .bento-item h3 { font-size: 1.4rem; }
                
                .marquee-wrapper { margin: 2rem 0; }
                .review-card { 
                    width: 280px !important; 
                    min-width: 280px; 
                    padding: 1.2rem;
                }

                .latest-insights { margin-top: 3rem; padding-top: 2rem; }
                .latest-insights h2 { font-size: 1.8rem; margin-bottom: 1.5rem; }
                .posts-grid { 
                    grid-template-columns: 1fr; 
                    gap: 1.5rem; 
                }
            }
		</style>
	</head>
	<body>
		<Header />
        <BackgroundEffects />
		<main>
			<section class="hero">
                <div class="hero-badge">Autonomous AI Factory v2.0</div>
				<h1>Maximize Your Potential with AI</h1>
				<p class="hero-subtitle">{SITE_DESCRIPTION}</p>
			</section>

            <section class="featured-sections">
                <h2>Our Ecosystem</h2>
                <div class="bento-grid">
                    <a href="/tools/aeo-audit" class="bento-item">
                        <div class="bento-badge">STABLE v2.0</div>
                        <div class="bento-content">
                            <h3>AEO Auditor 2.0</h3>
                            <p>Maximize your visibility in AI Search. Real-time scores and precision prompts for Perplexity, SearchGPT & ChatGPT.</p>
                        </div>
                        <div class="scan-container">
                            <img 
                                src="/assets/aeo-mockup.webp" 
                                alt="AEO Auditor" 
                                width="600" 
                                height="375" 
                                decoding="async"
                                loading="lazy"
                            />
                        </div>
                        <div class="bento-meta">
                            <span>LATENCY: 140MS</span>
                            <span>INDEXING: ENABLED</span>
                        </div>
                    </a>
                    <a href="/blog" class="bento-item">
                        <div class="small-card-content">
                            <div class="bento-badge" style="position: relative; top: 0; right: 0; width: fit-content; margin-bottom: 1rem;">DOCS</div>
                            <h3>Deep Guides</h3>
                            <p>Exhaustive technical deep-dives into agentic workflows.</p>
                        </div>
                        <div class="small-card-visual">
                            <img 
                                src="/assets/deep-guides.webp" 
                                alt="Deep Guides" 
                            />
                            <div class="grid-overlay" style="opacity: 0.1;"></div>
                        </div>
                    </a>
                    <a href="/about" class="bento-item">
                        <div class="small-card-content">
                            <div class="bento-badge" style="position: relative; top: 0; right: 0; width: fit-content; margin-bottom: 1rem;">REPO</div>
                            <h3>Architecture</h3>
                            <p>The code powering this hub.</p>
                        </div>
                        <div class="small-card-visual">
                            <img 
                                src="/assets/architecture.webp" 
                                alt="Architecture" 
                            />
                            <div class="grid-overlay" style="opacity: 0.1;"></div>
                        </div>
                    </a>
                </div>
            </section>

            <script is:inline>
                // Interactive Bento Mouse Effect
                document.querySelectorAll('.bento-item').forEach(item => {
                    item.addEventListener('mousemove', e => {
                        const rect = item.getBoundingClientRect();
                        const x = ((e.clientX - rect.left) / rect.width) * 100;
                        const y = ((e.clientY - rect.top) / rect.height) * 100;
                        item.style.setProperty('--x', `${x}%`);
                        item.style.setProperty('--y', `${y}%`);
                    });
                });
            </script>

            <!-- Testimonials Marquee -->
            <section class="marquee-wrapper">
                <div class="marquee-track">
                    <div class="review-card">
                        <div class="reviewer-header">
                            <div class="avatar" style="background: #FF9F1C">J</div>
                            <div class="reviewer-info"><div class="reviewer-name">James T.</div><div class="reviewer-role">Solo Founder</div></div>
                        </div>
                        <p class="review-text">“This is the only newsletter I actually open. The 'Digital Twin' guide was mind-blowing.”</p>
                    </div>
                    <div class="review-card">
                        <div class="reviewer-header">
                            <div class="avatar" style="background: #2EC4B6">A</div>
                            <div class="reviewer-info"><div class="reviewer-name">Aisha P.</div><div class="reviewer-role">AI Engineer</div></div>
                        </div>
                        <p class="review-text">“Precise, technical, and forward-thinking. This is where the alpha is.”</p>
                    </div>
                    <div class="review-card">
                        <div class="reviewer-header">
                            <div class="avatar" style="background: #E71D36">T</div>
                            <div class="reviewer-info"><div class="reviewer-name">Tom W.</div><div class="reviewer-role">SaaS Builder</div></div>
                        </div>
                        <p class="review-text">“I use the Tools directory as my homepage. It filters out so much noise.”</p>
                    </div>
                    <!-- REPEAT FOR LOOP -->
                    <div class="review-card">
                        <div class="reviewer-header">
                            <div class="avatar" style="background: #FF9F1C">J</div>
                            <div class="reviewer-info"><div class="reviewer-name">James T.</div><div class="reviewer-role">Solo Founder</div></div>
                        </div>
                        <p class="review-text">“This is the only newsletter I actually open. The 'Digital Twin' guide was mind-blowing.”</p>
                    </div>
                    <div class="review-card">
                        <div class="reviewer-header">
                            <div class="avatar" style="background: #2EC4B6">A</div>
                            <div class="reviewer-info"><div class="reviewer-name">Aisha P.</div><div class="reviewer-role">AI Engineer</div></div>
                        </div>
                        <p class="review-text">“Precise, technical, and forward-thinking. This is where the alpha is.”</p>
                    </div>
                    <div class="review-card">
                        <div class="reviewer-header">
                            <div class="avatar" style="background: #E71D36">T</div>
                            <div class="reviewer-info"><div class="reviewer-name">Tom W.</div><div class="reviewer-role">SaaS Builder</div></div>
                        </div>
                        <p class="review-text">“I use the Tools directory as my homepage. It filters out so much noise.”</p>
                    </div>
                </div>
            </section>

			<section class="latest-insights">
				<h2>Latest Insights</h2>
				<div class="posts-grid">
					{
						posts.map((post) => (
							<a href={`/blog/${post.id}`} class="post-card">
								<div class="post-image">
									<img 
                                        src={post.data.heroImage} 
                                        alt="" 
                                        loading="lazy" 
                                        decoding="async"
                                        width="400"
                                        height="200"
                                    />
								</div>
								<div class="post-content">
									<h3>{post.data.title}</h3>
									<p>{post.data.description}</p>
								</div>
							</a>
						))
					}
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>
